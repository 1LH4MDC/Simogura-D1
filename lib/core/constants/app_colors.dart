import 'package:flutter/material.dart';

class AppColors {
  // ── Warna Utama ──────────────────────────────────────
  static const Color primary    = Color(0xFF0C344D); // Navy gelap
  static const Color secondary  = Color(0xFF1A6B9A); // Biru medium
  static const Color accent     = Color(0xFF2FA8D5); // Biru muda

  // ── Warna Status Sensor ───────────────────────────────
  static const Color normal     = Color(0xFF4CAF50); // Hijau — kondisi normal
  static const Color warning    = Color(0xFFF4A623); // Kuning — peringatan
  static const Color danger     = Color(0xFFE53935); // Merah — bahaya

  // ── Warna Background ─────────────────────────────────
  static const Color background = Color(0xFFF4F6F8); // Background abu muda
  static const Color surface    = Color(0xFFFFFFFF); // Card putih

  // ── Warna Teks ───────────────────────────────────────
  static const Color textDark   = Color(0xFF1A2E44); // Teks gelap
  static const Color textLight  = Color(0xFF7A9BB0); // Teks abu
  static const Color textWhite  = Color(0xFFFFFFFF); // Teks putih
  static const Color textHint   = Color(0xFFAAAAAA); // Hint field

  // ── Warna Garis & Border ─────────────────────────────
  static const Color line       = Color(0xFFE0E0E0);
  static const Color border     = Color(0xFFE8EEF3);

  // ── Warna Ikon Sensor ────────────────────────────────
  static const Color iconSuhu   = Color(0xFF4FC3F7); // Biru muda — suhu
  static const Color iconPh     = Color(0xFF81C784); // Hijau muda — pH
  static const Color iconAmonia = Color(0xFFFFB74D); // Oranye — amonia
  static const Color iconAir    = Color(0xFF4FC3F7); // Biru — ketinggian air
  static const Color iconPakan  = Color(0xFFFF8A65); // Salmon — pakan

  // ── Warna Logout ─────────────────────────────────────
  static const Color logout     = Color(0xFFB92025);
}